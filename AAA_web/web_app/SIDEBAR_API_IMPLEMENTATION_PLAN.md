# 사이드바 API 연동 구현 계획서

## 📋 개요
lib 폴더의 사이드바에서 사용하는 모든 API를 파악하여 모바일 웹과 데스크톱 웹에서 동작하도록 구현합니다.
**AMQP는 제외**하고 나머지 모든 API를 연동합니다.

---

## 🔍 사이드바에서 사용하는 API 목록

### 1. 채팅 관련 API (이미 구현됨 ✅)
- `POST /getArchiveList` - 아카이브 목록 조회
- `POST /getSingleArchive` - 아카이브 상세 조회
- `POST /createArchive` - 새 아카이브 생성
- `POST /updateArchive` - 아카이브 이름 수정
- `POST /deleteArchive` - 아카이브 삭제
- `POST /chat` - AI 채팅 (스트리밍)
- `POST /streamChat/timeout` - 스트리밍 타임아웃
- `POST /streamChat/withModel` - 모델 선택 스트리밍

**구현 상태**: ✅ 완료 (`chatService.ts`)

---

### 2. 전자결재 관리 API (미구현 ❌)

#### 2.1 전자결재 문서 목록 조회
- **엔드포인트**: `POST /api/approval/list` (추정)
- **용도**: 사이드바 "전자결재" 메뉴 클릭 시 문서 목록 조회
- **필요 데이터**:
  - 사용자 ID
  - 문서 상태 필터 (전체/진행중/완료/반려)
  - 문서 타입 필터
- **응답 데이터**:
  - 문서 목록 (문서번호, 제목, 기안자, 기안일, 상태, 타입 등)

#### 2.2 전자결재 문서 상세 조회
- **엔드포인트**: `POST /api/approval/detail` (추정)
- **용도**: 문서 클릭 시 상세 정보 조회
- **필요 데이터**:
  - 문서 ID
  - 사용자 ID
- **응답 데이터**:
  - 문서 상세 정보
  - 결재선 정보
  - 첨부파일 목록
  - 결재 이력

#### 2.3 전자결재 상신
- **엔드포인트**: `POST /api/approval/submit` (추정)
- **용도**: 새 전자결재 문서 작성 및 상신
- **필요 데이터**:
  - 문서 제목
  - 문서 내용
  - 결재 종류
  - 결재선
  - 참조자 목록
  - 첨부파일
  - 긴급도
  - 예산 (해당 시)
- **응답 데이터**:
  - 상신 결과
  - 문서 ID

#### 2.4 전자결재 승인/반려
- **엔드포인트**: `POST /api/approval/approve` 또는 `/api/approval/reject` (추정)
- **용도**: 결재자 권한으로 문서 승인/반려
- **필요 데이터**:
  - 문서 ID
  - 승인/반려 여부
  - 의견/코멘트
- **응답 데이터**:
  - 처리 결과

#### 2.5 휴가 부여 상신 (특수)
- **엔드포인트**: `POST /api/approval/leave-grant` (추정)
- **용도**: 휴가 부여 상신 (hr_leave_grant 타입)
- **필요 데이터**:
  - 휴가 부여 대상자
  - 휴가 종류
  - 휴가 일수
  - 사유
  - 결재선
- **응답 데이터**:
  - 상신 결과

**구현 상태**: ❌ 미구현
**파일 위치**: `lib/ui/screens/electronic_approval_management_screen.dart`
**참고**: 현재 더미 데이터로 구현되어 있음

---

### 3. 휴가 관리 API (부분 구현 ⚠️)

#### 3.1 휴가관리 데이터 조회 ✅
- **엔드포인트**: `POST /leave/user/management`
- **구현 상태**: ✅ 완료 (`leaveService.ts`)
- **용도**: 휴가 현황, 결재진행 현황, 연도별 내역 조회

#### 3.2 월별 달력 조회 ✅
- **엔드포인트**: `POST /leave/user/management/myCalendar`
- **구현 상태**: ✅ 완료 (`leaveService.ts`)
- **용도**: 개인 달력 월별 조회

#### 3.3 연도별 휴가 내역 조회 ✅
- **엔드포인트**: `POST /leave/user/management/yearlyLeave`
- **구현 상태**: ✅ 완료 (`leaveService.ts`)
- **용도**: 연도 필터로 휴가 내역 조회

#### 3.4 전체 달력 조회 ✅
- **엔드포인트**: `POST /leave/user/management/totalCalendar`
- **구현 상태**: ✅ 완료 (`leaveService.ts`)
- **용도**: 부서 전체 휴가 달력 조회

#### 3.5 휴가 신청 ✅
- **엔드포인트**: `POST /leave/user/request`
- **구현 상태**: ✅ 완료 (`leaveService.ts`)
- **용도**: 휴가 신청

#### 3.6 휴가 취소 ✅
- **엔드포인트**: `POST /leave/user/cancel`
- **구현 상태**: ✅ 완료 (`leaveService.ts`)
- **용도**: 휴가 취소

#### 3.7 관리자용 휴가 관리 데이터 조회 ⚠️
- **엔드포인트**: `POST /leave/admin/management`
- **구현 상태**: ⚠️ 부분 구현 (타입 정의 필요)
- **용도**: 관리자용 휴가 관리 대장 조회

#### 3.8 관리자 승인/반려 처리 ✅
- **엔드포인트**: `POST /leave/admin/approval`
- **구현 상태**: ✅ 완료 (`leaveService.ts`)
- **용도**: 관리자 권한으로 휴가 승인/반려

#### 3.9 관리자 부서 달력 조회 ✅
- **엔드포인트**: `POST /leave/admin/deptCalendar`
- **구현 상태**: ✅ 완료 (`leaveService.ts`)
- **용도**: 관리자용 부서 달력 조회

#### 3.10 관리자 휴가 현황 조회 ⚠️
- **엔드포인트**: `POST /leave/admin/status`
- **구현 상태**: ⚠️ 미구현
- **용도**: 관리자용 휴가 현황 조회

#### 3.11 관리자 휴가 부여 ⚠️
- **엔드포인트**: `POST /leave/admin/grant`
- **구현 상태**: ⚠️ 미구현
- **용도**: 관리자 권한으로 휴가 부여

#### 3.12 휴가 잔여일 조회 ⚠️
- **엔드포인트**: `POST /api/leave/balance`
- **구현 상태**: ⚠️ 미구현
- **용도**: 사용자별 휴가 잔여일 조회

#### 3.13 연도별 휴가 현황 조회 ⚠️
- **엔드포인트**: `POST /leave/user/management/yearly`
- **구현 상태**: ⚠️ 미구현
- **용도**: 연도별 휴가 현황 상세 조회

#### 3.14 부서 휴가 이력 조회 ⚠️
- **엔드포인트**: `POST /leave/user/management/departmentHistory`
- **구현 상태**: ⚠️ 미구현
- **용도**: 부서별 휴가 이력 조회

**구현 상태**: ⚠️ 부분 구현 (약 60% 완료)
**파일 위치**: `leaveService.ts`

---

### 4. 사내AI 공모전 API (미구현 ❌)

#### 4.1 공모전 목록 조회
- **엔드포인트**: `POST /contest/management`
- **파라미터**:
  - `contest_type`: "test" (고정)
  - `view_type`: "random" | "view_count" | "votes"
  - `user_id`: 사용자 ID
  - `category`: 카테고리 필터 (빈값이면 전체)
  - `comments`: true (댓글 포함 여부)
- **응답 데이터**:
  - `documents`: 공모전 문서 목록
  - 각 문서: 제목, 내용, 이미지, 좋아요 수, 조회수, 댓글 등

#### 4.2 공모전 신청서 제출
- **엔드포인트**: `POST /contest/chat`
- **파라미터**:
  - `contest_type`: "test" (고정)
  - `user_id`: 사용자 ID
  - `message`: 사용자 채팅/신청 내용
  - `files`: 이미지 파일 (multipart/form-data)
  - `fileNames`: 파일명 리스트
- **응답 데이터**:
  - 제출 결과
  - 생성된 문서 ID

#### 4.3 좋아요/투표
- **엔드포인트**: `POST /contest/vote`
- **파라미터**:
  - `user_id`: 사용자 ID
  - `document_id`: 문서 ID
  - `action`: "like" | "unlike"
- **응답 데이터**:
  - 좋아요 결과
  - 현재 좋아요 수

#### 4.4 남은 투표 수 조회
- **엔드포인트**: `POST /contest/user/remainVotes`
- **파라미터**:
  - `user_id`: 사용자 ID
- **응답 데이터**:
  - `remain_votes`: 남은 투표 수

#### 4.5 사용자 정보 조회
- **엔드포인트**: `POST /contest/userInfo`
- **파라미터**:
  - `user_id`: 사용자 ID
- **응답 데이터**:
  - `name`: 이름
  - `department`: 부서
  - `job_position`: 직급

#### 4.6 사용자 제출 여부 체크
- **엔드포인트**: `POST /contest/user/submission` (추정)
- **파라미터**:
  - `user_id`: 사용자 ID
  - `contest_type`: "test" (기본값)
- **응답 데이터**:
  - `status`: 1 (제출함) | 0 (미제출)

**구현 상태**: ❌ 미구현
**파일 위치**: `lib/shared/services/contest_api_service.dart`
**참고**: Flutter에서는 완전히 구현되어 있음

---

### 5. 선물 API (부분 구현 ⚠️)

#### 5.1 선물 보내기 ✅
- **엔드포인트**: `POST /send_birthday_gift`
- **구현 상태**: ✅ 완료 (`giftService.ts`)
- **파라미터**:
  - `id`: 알림 ID
  - `goods_code`: 상품 코드
  - `user_id`: 사용자 ID
  - `queue_name`: "birthday" | "event"
- **응답 데이터**:
  - 선물 전송 결과

#### 5.2 받은 선물 목록 조회 ⚠️
- **엔드포인트**: `POST /checkGifts` 또는 `/queue/checkGifts`
- **구현 상태**: ⚠️ 미구현
- **용도**: 사용자가 받은 선물 목록 조회
- **파라미터**:
  - `user_id`: 사용자 ID
- **응답 데이터**:
  - 선물 목록 (쿠폰 이미지 URL, 상품명, 수신일 등)

#### 5.3 모바일로 내보내기 ✅
- **엔드포인트**: `POST /send_to_mobile`
- **구현 상태**: ✅ 완료 (`giftService.ts`)
- **파라미터**:
  - `couponImgUrl`: 쿠폰 이미지 URL
- **응답 데이터**:
  - `code`: 결과 코드
  - `message`: 메시지
  - `result`: 결과

**구현 상태**: ⚠️ 부분 구현 (약 66% 완료)
**파일 위치**: `giftService.ts`

---

## 📝 구현 계획

### Phase 1: 전자결재 관리 API 구현 (우선순위: 높음)

#### 1.1 API 서비스 파일 생성
- **파일**: `web_app/src/services/approvalService.ts`
- **구현 항목**:
  - `getApprovalList()` - 문서 목록 조회
  - `getApprovalDetail()` - 문서 상세 조회
  - `submitApproval()` - 전자결재 상신
  - `approveApproval()` - 승인 처리
  - `rejectApproval()` - 반려 처리
  - `submitLeaveGrant()` - 휴가 부여 상신

#### 1.2 타입 정의
- **파일**: `web_app/src/types/approval.ts`
- **타입 정의**:
  - `ApprovalDocument` - 문서 정보
  - `ApprovalDetail` - 문서 상세 정보
  - `ApprovalLine` - 결재선 정보
  - `ApprovalRequest` - 상신 요청 데이터
  - `ApprovalResponse` - API 응답 타입

#### 1.3 페이지/컴포넌트 업데이트
- **파일**: `web_app/src/pages/ApprovalPage.tsx`
- **작업 내용**:
  - 더미 데이터 제거
  - 실제 API 연동
  - 로딩/에러 처리
  - 모바일/데스크톱 반응형 처리

---

### Phase 2: 휴가 관리 API 추가 구현 (우선순위: 중간)

#### 2.1 누락된 API 추가
- **파일**: `web_app/src/services/leaveService.ts`
- **추가 구현**:
  - `getAdminLeaveStatus()` - 관리자 휴가 현황 조회
  - `grantLeave()` - 관리자 휴가 부여
  - `getLeaveBalance()` - 휴가 잔여일 조회
  - `getYearlyLeaveStatus()` - 연도별 휴가 현황 조회
  - `getDepartmentHistory()` - 부서 휴가 이력 조회

#### 2.2 타입 정의 추가
- **파일**: `web_app/src/types/leave.ts`
- **추가 타입**:
  - `AdminLeaveStatus`
  - `LeaveGrantRequest`
  - `LeaveBalance`
  - `YearlyLeaveStatus`
  - `DepartmentHistory`

---

### Phase 3: 사내AI 공모전 API 구현 (우선순위: 중간)

#### 3.1 API 서비스 파일 생성
- **파일**: `web_app/src/services/contestService.ts`
- **구현 항목**:
  - `getContestList()` - 공모전 목록 조회
  - `submitContest()` - 공모전 신청서 제출
  - `voteContest()` - 좋아요/투표
  - `getRemainingVotes()` - 남은 투표 수 조회
  - `getUserInfo()` - 사용자 정보 조회
  - `checkUserSubmission()` - 제출 여부 체크

#### 3.2 타입 정의
- **파일**: `web_app/src/types/contest.ts`
- **타입 정의**:
  - `ContestDocument` - 공모전 문서
  - `ContestListRequest` - 목록 조회 요청
  - `ContestSubmitRequest` - 신청서 제출 요청
  - `VoteRequest` - 투표 요청
  - `UserInfo` - 사용자 정보

#### 3.3 페이지 업데이트
- **파일**: `web_app/src/pages/ContestPage.tsx`
- **작업 내용**:
  - 실제 API 연동
  - 이미지 업로드 처리
  - 좋아요/투표 기능
  - 모바일/데스크톱 반응형 처리

---

### Phase 4: 선물 API 추가 구현 (우선순위: 낮음)

#### 4.1 누락된 API 추가
- **파일**: `web_app/src/services/giftService.ts`
- **추가 구현**:
  - `getReceivedGifts()` - 받은 선물 목록 조회

#### 4.2 타입 정의 추가
- **파일**: `web_app/src/types/gift.ts`
- **추가 타입**:
  - `ReceivedGift` - 받은 선물 정보
  - `GiftListResponse` - 선물 목록 응답

---

### Phase 5: 통합 및 테스트 (우선순위: 높음)

#### 5.1 공통 API 클라이언트 설정
- **파일**: `web_app/src/services/api.ts`
- **확인 사항**:
  - 모든 API 엔드포인트가 올바르게 프록시 설정되어 있는지 확인
  - 인증 토큰 처리 확인
  - 에러 핸들링 통일

#### 5.2 모바일/데스크톱 공통 처리
- **확인 사항**:
  - 모든 페이지가 반응형으로 작동하는지 확인
  - 모바일에서 API 호출이 정상 작동하는지 확인
  - 데스크톱에서도 동일하게 작동하는지 확인

#### 5.3 에러 처리 및 로딩 상태
- **확인 사항**:
  - 모든 API 호출에 로딩 상태 표시
  - 에러 발생 시 사용자에게 적절한 메시지 표시
  - 네트워크 오류 처리

---

## 🔧 기술 스택

### 기존 사용 중
- **HTTP 클라이언트**: Axios
- **상태 관리**: Zustand (일부), React Context
- **UI 프레임워크**: Material-UI
- **타입 시스템**: TypeScript

### 추가 필요 사항
- **파일 업로드**: FormData (multipart/form-data)
- **이미지 처리**: FileReader API
- **날짜 처리**: dayjs (이미 사용 중)

---

## 📅 구현 일정 (예상)

1. **Phase 1 (전자결재)**: 3-4일
   - API 서비스 구현: 1일
   - 타입 정의 및 페이지 업데이트: 2일
   - 테스트 및 버그 수정: 1일

2. **Phase 2 (휴가 관리 추가)**: 2일
   - 누락된 API 추가: 1일
   - 테스트: 1일

3. **Phase 3 (공모전)**: 4-5일
   - API 서비스 구현: 2일
   - 타입 정의 및 페이지 업데이트: 2일
   - 이미지 업로드 처리: 1일

4. **Phase 4 (선물 추가)**: 1일
   - API 추가 및 테스트: 1일

5. **Phase 5 (통합 테스트)**: 2일
   - 전체 통합 테스트: 1일
   - 버그 수정 및 최적화: 1일

**총 예상 기간**: 12-14일

---

## ⚠️ 주의사항

1. **AMQP 제외**: 실시간 알림 기능은 AMQP를 사용하므로 웹에서는 제외
2. **인증 처리**: 모든 API 호출 시 인증 토큰이 필요할 수 있음
3. **CORS 설정**: vite.config.ts의 프록시 설정 확인 필요
4. **에러 처리**: 네트워크 오류, 타임아웃 등 모든 경우 처리
5. **모바일 최적화**: 모바일에서도 동일하게 작동하도록 반응형 처리

---

## 📚 참고 파일

### Flutter (lib 폴더)
- `lib/shared/services/leave_api_service.dart` - 휴가 API
- `lib/shared/services/contest_api_service.dart` - 공모전 API
- `lib/shared/services/gift_service.dart` - 선물 API
- `lib/shared/services/api_service.dart` - 채팅 API
- `lib/ui/screens/electronic_approval_management_screen.dart` - 전자결재 화면
- `lib/ui/screens/leave_management_screen.dart` - 휴가 관리 화면
- `lib/ui/screens/contest_screen.dart` - 공모전 화면

### 웹 앱 (web_app 폴더)
- `web_app/src/services/chatService.ts` - 채팅 API (완료)
- `web_app/src/services/leaveService.ts` - 휴가 API (부분 완료)
- `web_app/src/services/giftService.ts` - 선물 API (부분 완료)
- `web_app/src/pages/ApprovalPage.tsx` - 전자결재 페이지 (더미 데이터)
- `web_app/src/pages/ContestPage.tsx` - 공모전 페이지 (더미 데이터)

---

## ✅ 체크리스트

### 전자결재 관리
- [ ] API 서비스 파일 생성
- [ ] 타입 정의
- [ ] 문서 목록 조회 API
- [ ] 문서 상세 조회 API
- [ ] 전자결재 상신 API
- [ ] 승인/반려 API
- [ ] 휴가 부여 상신 API
- [ ] 페이지 업데이트 (더미 데이터 제거)
- [ ] 모바일/데스크톱 반응형 처리

### 휴가 관리 추가
- [ ] 관리자 휴가 현황 조회 API
- [ ] 관리자 휴가 부여 API
- [ ] 휴가 잔여일 조회 API
- [ ] 연도별 휴가 현황 조회 API
- [ ] 부서 휴가 이력 조회 API

### 사내AI 공모전
- [ ] API 서비스 파일 생성
- [ ] 타입 정의
- [ ] 공모전 목록 조회 API
- [ ] 공모전 신청서 제출 API (이미지 업로드 포함)
- [ ] 좋아요/투표 API
- [ ] 남은 투표 수 조회 API
- [ ] 사용자 정보 조회 API
- [ ] 제출 여부 체크 API
- [ ] 페이지 업데이트 (더미 데이터 제거)
- [ ] 이미지 업로드 UI 구현

### 선물 추가
- [ ] 받은 선물 목록 조회 API
- [ ] 타입 정의 추가

### 통합 및 테스트
- [ ] 모든 API 프록시 설정 확인
- [ ] 인증 토큰 처리 확인
- [ ] 에러 핸들링 통일
- [ ] 모바일 테스트
- [ ] 데스크톱 테스트
- [ ] 네트워크 오류 테스트

